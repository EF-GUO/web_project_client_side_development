import { defineConfig } from 'windicss/helpers'

export default defineConfig({
  darkMode: 'class', // or 'media'
  theme: {
    colors:{
      cake:{
        nav:'#5d4b33',
      },
    },
    extend: {},
  },
  plugins: [],
})